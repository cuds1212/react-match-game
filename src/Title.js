import React from 'react';

import './Title.css';

// Title is a stateless component
const Title = (props) => {
    return (
        <div className='title'>
            <h1>React Match Game</h1>
        </div>
    )
};

export default Title;